import React from "react";

const AddTask = () => {
  return (
    <div>
      <form action="">
        <input type="text" placeholder="Primary" class="input input-primary" />
        <button class="btn btn-dash bg-secondary btn-primary">Primary</button>
      </form>
    </div>
  );
};

export default AddTask;
